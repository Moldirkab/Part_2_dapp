# Quote Registry | Web3 DApp

A simple Web3 decentralized application (DApp) that lets users connect their Ethereum wallet via MetaMask and fetch a blockchain quote from a smart contract. Built with **Vite** and **ethers.js**.

## Features

- Connect MetaMask wallet
- Display wallet address
- Fetch and display a blockchain quote
- Error handling for connection/network issues